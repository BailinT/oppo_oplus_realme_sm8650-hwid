# 当前会话交接

## 最新状态（2026-07-23）
- 已从 `BailinT/oppo_oplus_realme_sm8650` 主分支克隆独立测试副本。
- 原本地目录 `oppo_oplus_realme_sm8650-main` 未修改。
- 已创建并推送公开新仓库：`https://github.com/BailinT/oppo_oplus_realme_sm8650-hwid`。
- 新仓库是独立仓库，不是 GitHub fork；`origin` 指向新仓库。
- 原仓库保留为 `upstream` fetch 来源，`upstream` push URL 为 `DISABLED`。
- 已加入 HWID 全白名单内核校验，当前只允许 `abbe0d2c`。
- GitHub 已识别全部 workflow，7 个内核构建 workflow 均为 `active`，Actions 权限已启用。
- 功能提交：`9ee14ced1d3922b7776a0e0c2efd8f0696dde8f4`。

## 实现规则
- `hwid/allowlist.txt` 的所有有效行都会编译进同一个内核。
- 优先读取 bootconfig/cmdline 的 `androidboot.chipid`，回退到 `oplusboot.serialno`。
- 缺失或白名单外 HWID 会 panic，`panic_timeout=1` 后自动重启。
- 不使用 workflow HWID 输入，白名单文件是唯一允许设备来源。

## 注意事项
- 尚未完成真实 6.1 内核构建和真机刷入验证。
- 刷入前保留原 boot 镜像以及 fastboot/recovery 回滚路径。
- 已推送到新仓库；未修改原仓库，未触发或 cancel GitHub Actions。
